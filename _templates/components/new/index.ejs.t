---
to: src/components/<%= name %>/index.jsx
---
import React from 'react';
import PropTypes from 'prop-types';

function <%= name %>(props) {
  return (
    <div></div>
  );
}

<%= name %>.propTypes = {
};
<%= name %>.defaultProps = {
};

export default <%= name %>;
